{
  "name": "hm-delbestilling",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.6.2"
  },
  "scripts": {
    "dev": "npm run --prefix client dev",
    "playwright": "npm run --prefix client playwright",
    "playwright:ui": "npm run --prefix client playwright:ui",
    "build": "concurrently --kill-others-on-fail \"npm run --prefix client build\" \"npm run --prefix server build\""
  }
}
